define(function(){
  var config = {
    lobby: {
      chatHistoryMaximumNumber: 20
    },
    UI: {
      card: {
        visibleWidth: 40,
        width: 120
      }
    }
  };

  return config;
});
